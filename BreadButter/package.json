{
  "name": "breadbutter-talent-matchmaker",
  "version": "1.0.0",
  "description": "AI-powered talent matchmaking engine for BreadButter",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup:db": "cd server && npm run setup:db"
  },
  "keywords": ["talent", "matchmaking", "AI", "breadbutter"],
  "author": "BreadButter Founding Engineer Candidate",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
